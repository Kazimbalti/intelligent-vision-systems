# OpenCV lab - Python libraries
# Install inside the virtual environment with:   pip install -r requirements.txt
# Tested September 2026 with Python 3.10 - 3.12, versions shown on the right.

numpy>=1.26          # arrays: every image is a NumPy array        (tested 2.4.6)
opencv-python>=4.9   # OpenCV with GUI windows and video support   (tested 4.14 and 5.0.0)
matplotlib>=3.8      # plots and histograms in notebooks           (tested 3.11.2)
jupyterlab>=4.0      # notebooks for exploring (optional)          (tested 4.6.3)

# Do NOT also install opencv-contrib-python or opencv-python-headless:
# the three packages overwrite each other. Pick exactly one.
